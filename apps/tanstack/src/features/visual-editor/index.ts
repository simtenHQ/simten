/**
 * Editor Feature Exports
 *
 * The EditorWorkspace shell. All other editor components, stores, hooks,
 * and types are available via `@turing-incomplete/ui/editor`.
 */

export { EditorWorkspace } from './components/EditorWorkspace';
