---
"@simten/ui": patch
---

Expose the shadcn tooltip via a new `@simten/ui/primitives/tooltip` export, and add `cursor-pointer` to the `Button` base so all buttons show the pointer on hover (disabled ones excluded).
