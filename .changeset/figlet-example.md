---
"@simten/core": patch
---

Add an npm example ("npm → ROM") to the bundled catalog: runs the figlet package at build time to render ASCII art, bakes it into a ROM, and streams it to a console. Demonstrates resolving a real npm library in a circuit (editor/canvas resolve it via esm.sh).
